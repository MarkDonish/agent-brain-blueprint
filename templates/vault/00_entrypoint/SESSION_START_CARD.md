# Session Start Card

This is a shared working memory vault, not a runtime configuration directory.

1. Read this card and the relevant project overview.
2. Read current work, handoff, validation, decisions, and source indexes in order.
3. Use retrieval to find candidates; reopen source Markdown before relying on it.
4. Create a narrow session claim before editing shared records in a multi-agent task.
5. Never store secrets, raw conversations, databases, logs, or customer data here.
