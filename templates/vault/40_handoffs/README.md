# Cross-Agent Handoffs

Use this directory for vault-level handoffs that span more than one project.

Session claims live in `session_claims/` and should stay short, narrow, and free
of secrets. Claim Markdown files are ignored by Git by default so private claims
are not published accidentally.
