{
  "name": "handoff",
  "description": "Project handoff record.",
  "required": ["memory_type", "owner", "next_action"],
  "optional": ["title", "from", "to", "created_at", "source", "confidence", "freshness", "scope", "risk_boundary", "next_review", "status"],
  "fields": {
    "memory_type": {"type": "enum", "enum": "memory_type"},
    "owner": {"type": "string"},
    "next_action": {"type": "string"},
    "from": {"type": "string"},
    "to": {"type": "string"}
  }
}
