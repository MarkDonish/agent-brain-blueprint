{
  "name": "memory_record",
  "description": "Provenance-bearing durable fact or decision.",
  "required": [
    "memory_type",
    "source",
    "confidence",
    "freshness",
    "scope",
    "risk_boundary",
    "next_review",
    "owner"
  ],
  "optional": [
    "title",
    "created_at",
    "updated_at",
    "source_path_or_url",
    "record_id",
    "record_type",
    "knowledge_type",
    "state",
    "valid_from",
    "review_after",
    "verified_at",
    "supersedes",
    "related_records",
    "validation_refs"
  ],
  "fields": {
    "memory_type": {"type": "enum", "enum": "memory_type"},
    "source": {"type": "string"},
    "confidence": {"type": "enum", "enum": "confidence"},
    "freshness": {"type": "enum", "enum": "freshness"},
    "scope": {"type": "enum", "enum": "scope"},
    "risk_boundary": {"type": "enum", "enum": "risk_boundary"},
    "next_review": {"type": "string"},
    "owner": {"type": "string"},
    "title": {"type": "string"},
    "source_path_or_url": {"type": "string"},
    "record_id": {"type": "record_id"},
    "record_type": {"type": "enum", "enum": "record_type"},
    "knowledge_type": {"type": "enum", "enum": "knowledge_type"},
    "state": {"type": "enum", "enum": "lifecycle_state"},
    "valid_from": {"type": "string"},
    "review_after": {"type": "string"},
    "verified_at": {"type": "string"},
    "supersedes": {"type": "list", "item_type": "string"},
    "related_records": {"type": "list", "item_type": "string"},
    "validation_refs": {"type": "list", "item_type": "string"}
  }
}
