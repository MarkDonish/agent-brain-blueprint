# Risk-pattern matches whose matched text contains one of these substrings
# are suppressed. Hard secrets (keys, tokens) are still reported.
# demo / docs placeholders
example-app
demo-notes-app
demo-user
demo-agent
codex-cli
claude-code
/path/to/
your-account
localhost
127.0.0.1
0.0.0.0
