## Description

Briefly describe the change, rationale, and which agent host / workflow it supports.

## Checklist

- [ ] I ran `make verify` and all unit tests, demo vault checks, and CLI smoke tests pass.
- [ ] I ran `python3 scripts/check_privacy_scan.py .` and verified NO credentials, tokens, or private paths are committed.
- [ ] Markdown remains canonical — no mandatory database or third-party service dependencies added.
- [ ] If changing CLI commands or schemas, corresponding tests and documentation under `docs/` were updated.
