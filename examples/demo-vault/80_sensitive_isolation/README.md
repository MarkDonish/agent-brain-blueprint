# Sensitive Isolation

This directory is a hard boundary. Nothing placed here belongs in a public
repository, shared backup, or casual handoff.

Keep:

- credentials, tokens, cookies, OAuth material, and `.env` files out of the vault
- private personal material outside any published template checkout
- customer data, invoices, and account identifiers offline and access-controlled

If material must be referenced, store only a private pointer outside Git.
